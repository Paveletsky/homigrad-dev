local client = LocalPlayer()

-- fundot.character = fundot.character or nil

-- local function CharacterMenu()

--     local m = vgui.Create('DFrame')
--     fundot.character = m
--     m:SetSize(600, 700)
--     m:SetPos(0, m:GetTall() / 4)

-- end

-- if fundot.character then fundot.character:Remove() end
-- CharacterMenu() 